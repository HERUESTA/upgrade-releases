# This Week in Rails 2026/03/20

- 記事: https://rubyonrails.org/2026/3/20/this-week-in-rails
- PR件数: 6件

---

## PR一覧

| PR | タイトル | カテゴリ | 選んだ |
|----|---------|---------|--------|
| [#57000](https://github.com/rails/rails/pull/57000) | Batch SQL statements when creating tables | パフォーマンス | ★ |
| [#56992](https://github.com/rails/rails/pull/56992) | Deprecate `require_dependency` | 非推奨 | - |
| [#56972](https://github.com/rails/rails/pull/56972) | MySQL lock options and algorithm extension | DB | - |
| [#56967](https://github.com/rails/rails/pull/56967) | Dockerfile build performance optimization | DevExp | - |
| [#56945](https://github.com/rails/rails/pull/56945) | PostgreSQL RESET support in read-only queries | DB | - |
| [#56926](https://github.com/rails/rails/pull/56926) | Action Text block child element support | 機能 | - |

---

## 深掘り: PR#57000 Batch SQL statements when creating tables

### 背景

- **何が問題だったか**: スキーマ読み込み時（`db:schema:load`）に、テーブルを1件作るたびにSQLを1件ずつ実行していた。1000件以上のテーブルを持つDBでは約120秒かかっていた
- **何を解決するか**: `create_table` 内で生成されるSQLをまとめて `execute_batch` でバッチ実行することで、約25秒まで短縮（約5倍高速化）

### テストdiff

```ruby
# 現在のadapterがPostgreSQLAdapterだったら
if current_adapter?(:PostgreSQLAdapter)
  def test_create_table_uses_batched_statements
    # connection = attr_reader :connection, :table_name
    # create_tableでtestingsというテーブル名が追加される
    # fooというstringのカラムが追加される
    connection.create_table :testings do |t|
      t.column :foo, :string
    end

    # DDLが1回実行されることを検証する
    assert_queries_count(1, include_schema: true) do
      connection.create_table :testings, comment: "This is a table", force: true do |t|
        t.column :foo, :string, comment: "This is a column"
        t.column :bar, :string
        t.index :foo, comment: "This is an index"
        t.index :bar
      end
    end

    # テーブルが存在するか
    assert connection.table_exists?(:testings)
    # Indexが存在するか
    assert connection.index_exists?(:testings, :foo)
    # barカラムが存在するか？
    assert connection.index_exists?(:testings, :bar)
    # テーブルコメントが存在するか？
    assert_equal "This is a table", connection.table_comment(:testings)
    assert_equal "This is a column", connection.columns(:testings).find { |c| c.name == "foo" }.comment
    assert_equal "This is an index", connection.indexes(:testings).find { |i| i.columns == ["foo"] }.comment
  end
end
```

- 何を検証している:PostgreSQLを実行した際に、カラムやIndex、テーブルコメントが付与されるかどうか
- 期待する動作:１回DDLが実行されカラム・Index・テーブルコメントが生成される

### 実装diff

```diff
- execute "DROP TABLE#{' IF EXISTS' if options[:if_exists]} #{table_names.map { |table_name| quote_table_name(table_name) }.join(', ')}#{' CASCADE' if options[:force] == :cascade}"
+ execute drop_table_sql(*table_names, **options)

- comment = extract_new_comment_value(comment_or_changes)
- execute "COMMENT ON COLUMN #{quote_table_name(table_name)}.#{quote_column_name(column_name)} IS #{quote(comment)}"
+ execute change_column_comment_sql(table_name, column_name, comment_or_changes)

- comment = extract_new_comment_value(comment_or_changes)
- execute "COMMENT ON TABLE #{quote_table_name(table_name)} IS #{quote(comment)}"
+ execute change_table_comment_sql(table_name, comment_or_changes)

- execute "COMMENT ON INDEX #{quote_column_name(index.name)} IS #{quote(index.comment)}" if index.comment
+ execute change_index_comment_sql(index) if index.comment
```

- 修正のポイント:
- なぜこの実装か:120秒かかる実装を25秒に短縮するため

### 学び

- 一言:

---

## 📝 用語メモ

<!--
知らなかった用語・APIをここにメモ
例:
- **execute_batch**: 複数のSQLをまとめて実行するメソッド
-->
